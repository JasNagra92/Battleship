function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}
function checkHorizontalValid(length, y) {
  if (y + length < 11) {
    return true;
  }
}
function checkVerticalValid(length, x) {
  if (x + length < 11) {
    return true;
  }
}
export { getRandomInt, checkHorizontalValid, checkVerticalValid };
